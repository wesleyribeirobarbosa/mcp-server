# =============================================================================
# Configuração da Aplicação Web - Smart Cities MCP
# =============================================================================

# Porta do servidor Express (padrão: 3001)
PORT=3001

# OpenRouter API Configuration
# Obtenha sua chave em: https://openrouter.ai/keys
# Esta chave é OPCIONAL - a aplicação funciona sem ela usando análise básica
OPENROUTER_API_KEY=your_openrouter_api_key_here

# MongoDB Configuration (usado pelo servidor MCP)
# Normalmente não precisa alterar se usando a configuração padrão
MONGO_URI=mongodb://localhost:27017/admin

# Logging Level (opcional)
LOG_LEVEL=info

# =============================================================================
# INSTRUÇÕES:
# =============================================================================
# 1. Copie este arquivo para .env
# 2. Substitua os valores conforme necessário
# 3. Para OpenRouter: configure apenas se quiser processamento avançado de prompts
# 4. O sistema funciona completamente sem a chave OpenRouter
#
# Exemplo:
# cp .env.example .env
# nano .env  # edite os valores
